{
  "name": "vscline-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "tsc && node --test tests/*.test.js && node scripts/architectureChecks.js && node scripts/parityChecks.js",
    "build": "node scripts/cleanDist.js && tsc && node scripts/copyRuntime.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@cline/sdk": "^0.0.43"
  }
}
