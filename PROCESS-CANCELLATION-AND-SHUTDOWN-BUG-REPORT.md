# 진행 중 대화 취소와 종료 처리 버그 보고서

## 문서 목적

이 문서는 2026-07-16에 발견한 LIG VS의 진행 중 대화 및 백그라운드 작업 종료 문제를 개발팀에 전달하기 위한 버그 보고서다.

이 보고서는 현재 저장소 코드의 구현 원인이나 수정 위치를 분석하지 않는다. 실제 배포본과 저장소 최신 코드가 다를 수 있으므로, 사용 중 관찰한 증상과 반드시 보장되어야 할 동작만 기록한다.

## 심각도

**높음 / 안전성과 데이터 무결성에 영향을 줄 수 있는 문제**

진행 중인 대화가 화면에서 중지되거나 Visual Studio가 종료된 것처럼 보여도 실제 작업과 관련 프로세스가 계속 실행된다면 다음 부작용이 발생할 수 있다.

- 사용자가 중지했다고 생각한 뒤에도 파일이 계속 변경될 수 있다.
- 터미널 명령, 도구 호출, MCP 호출, 외부 시스템 작업이 계속될 수 있다.
- Visual Studio를 다시 실행했을 때 이전 작업의 결과가 뒤늦게 나타날 수 있다.
- 보이지 않는 백그라운드 프로세스가 CPU, 메모리, 네트워크, 파일 핸들을 계속 사용할 수 있다.
- 같은 작업을 다시 실행하면 이전 작업과 새 작업이 중복 수행될 수 있다.
- 외부 시스템에 중복 변경 또는 사용자가 의도하지 않은 변경이 발생할 수 있다.

## 발견 내용 1: 일반적인 대화 종료 동작

진행 중인 대화에서 다음 동작을 수행한 경우, 대화 프로세스와 그 대화가 시작한 모든 백그라운드 작업이 제대로 종료되는지 의심되는 현상이 있다.

1. 중지 버튼 클릭
2. 취소 버튼 클릭
3. 현재 세션에서 나가기
4. 뒤로가기
5. 다른 대화 또는 다른 메뉴로 이동
6. LIG VS가 표시된 Visual Studio 도구 창 닫기

이러한 동작 이후에는 화면만 바뀌는 것이 아니라 진행 중인 실제 작업에도 종료 요청이 전달되어야 한다. 종료가 완료되지 않았다면 사용자에게 계속 실행 중이라는 사실이나 종료 실패 상태를 명확히 알려야 한다.

현재 관찰로는 UI에서 대화를 벗어나거나 중지한 뒤에도 관련 작업이 백그라운드에서 계속될 가능성이 있다.

## 발견 내용 2: Visual Studio의 큰 종료 이벤트

다음과 같은 큰 종료 이벤트에서는 LIG VS 자체와 LIG VS가 시작하거나 사용 중인 관련 프로세스 및 작업이 반드시 안전하게 종료되어야 한다.

1. LIG VS가 열린 Visual Studio 창 닫기
2. Visual Studio 정상 종료
3. Visual Studio 전체 창 종료
4. Visual Studio 비정상 종료
5. 작업 관리자 등을 이용한 Visual Studio 강제 종료
6. Visual Studio 또는 LIG VS가 응답하지 않는 상태에서의 종료
7. Windows 로그오프, 종료, 재시작 등으로 Visual Studio가 닫히는 상황

현재 관찰로는 이런 큰 종료 동작에서도 LIG VS와 관련된 모든 프로세스 및 작업이 함께 종료된다는 보장이 부족해 보인다.

특히 Visual Studio 화면이 닫힌 뒤에도 다음 항목이 남아 실행될 가능성을 확인할 필요가 있다.

- 진행 중인 AI 대화 요청
- 응답 스트리밍
- 터미널 명령과 그 명령이 시작한 하위 프로세스
- 백그라운드 명령
- MCP 서버 또는 MCP 도구 호출
- Hook 스크립트
- 브라우저 또는 WebView 관련 작업
- Sidecar 및 Sidecar가 시작한 작업
- 파일 읽기·쓰기 또는 외부 서비스 변경 작업
- 예약 작업, 타이머, 재시도 작업

## 기대 동작

### 중지 또는 취소

- 현재 응답 생성을 즉시 중지해야 한다.
- 실행 중인 도구 호출에 취소가 전파되어야 한다.
- 터미널 명령과 그 하위 작업이 종료되어야 한다.
- 외부 시스템 변경 작업이 더 진행되지 않아야 한다.
- 실제 종료가 확인된 뒤 UI에 취소 완료를 표시해야 한다.
- 종료하지 못한 작업이 있으면 성공으로 표시하지 말고 실패 또는 잔존 작업을 알려야 한다.
- 취소 후 같은 세션을 계속 사용할 수 있다면, 세션 상태가 손상되지 않아야 한다.

### 세션 나가기 또는 뒤로가기

- 단순히 화면만 숨기고 작업을 계속할지, 작업을 취소할지 정책이 명확해야 한다.
- 백그라운드 실행이 제품의 의도라면 사용자에게 계속 실행 중임을 표시해야 한다.
- 백그라운드 실행이 의도가 아니라면 화면 이동 전에 종료하고 완료를 확인해야 한다.
- 다른 세션으로 이동한 뒤 이전 세션의 작업이 보이지 않게 계속되어서는 안 된다.

### LIG VS 도구 창 닫기

- 창 닫기와 단순 숨김의 동작 차이가 명확해야 한다.
- 창을 닫으면 활성 대화와 관련 작업을 종료할지, 백그라운드에서 유지할지 사용자에게 일관되게 알려야 한다.
- 작업을 유지한다면 다시 열었을 때 정확한 실행 상태가 보여야 한다.
- 작업을 종료한다면 모든 관련 작업의 종료 확인이 필요하다.

### Visual Studio 정상 종료

- 새로운 대화와 도구 실행을 더 이상 받지 않아야 한다.
- 진행 중인 대화에 취소 요청을 전달해야 한다.
- 실행 중인 명령, 도구, MCP, Hook, Sidecar 관련 작업을 순서대로 종료해야 한다.
- 필요한 상태와 로그를 저장해야 한다.
- 정상 종료가 일정 시간 안에 끝나지 않으면 남은 작업을 강제로 정리해야 한다.
- 종료 후 관련 프로세스가 남아 있지 않아야 한다.

### Visual Studio 강제 종료

- 강제 종료에서는 일반적인 종료 콜백이 실행되지 않을 수 있다는 전제로 보호 장치가 필요하다.
- Visual Studio가 사라진 뒤 LIG VS가 소유한 프로세스와 작업이 고아 프로세스로 남지 않아야 한다.
- 다음 시작 시 이전 실행이 비정상 종료되었음을 감지하고 안전하게 복구해야 한다.
- 이전 작업이 사용자 확인 없이 자동으로 계속 수행되어서는 안 된다.

## 우선 확인이 필요한 재현 시나리오

### 시나리오 A: 응답 생성 중 중지

1. 긴 응답 또는 여러 도구 호출이 필요한 대화를 시작한다.
2. 응답 스트리밍 중 중지 또는 취소 버튼을 누른다.
3. UI가 중지된 뒤에도 토큰 출력, 도구 호출, 파일 변경이 계속되는지 확인한다.
4. 같은 세션에 새 메시지를 보내 이전 작업과 겹치는지 확인한다.

### 시나리오 B: 터미널 명령 중 중지

1. 장시간 실행되는 터미널 명령을 대화에서 시작한다.
2. 해당 명령이 다른 하위 프로세스도 시작하도록 구성한다.
3. 중지 또는 취소 버튼을 누른다.
4. 터미널의 부모 프로세스뿐 아니라 하위 프로세스도 종료되는지 확인한다.
5. 파일 변경과 네트워크 활동이 더 이상 진행되지 않는지 확인한다.

### 시나리오 C: 대화 도중 뒤로가기

1. 도구 호출이 진행되는 대화를 시작한다.
2. 뒤로가기 또는 다른 메뉴로 이동한다.
3. 이전 대화가 계속 실행되는지 확인한다.
4. 계속 실행된다면 UI 어디에서 진행 상태와 취소 기능을 제공하는지 확인한다.

### 시나리오 D: LIG VS 도구 창 닫기

1. 진행 중인 대화를 시작한다.
2. LIG VS 도구 창을 닫는다.
3. 관련 대화, 도구, 명령이 계속되는지 확인한다.
4. 도구 창을 다시 열었을 때 실제 실행 상태가 정확히 복원되는지 확인한다.

### 시나리오 E: Visual Studio 정상 종료

1. 장시간 대화와 터미널 또는 MCP 작업을 실행한다.
2. Visual Studio의 닫기 버튼으로 정상 종료한다.
3. Visual Studio 종료 후 관련 프로세스가 남아 있는지 확인한다.
4. 파일, 네트워크, 외부 시스템 변경이 계속되는지 확인한다.
5. Visual Studio를 다시 실행해 이전 작업이 뒤늦게 완료된 상태로 나타나는지 확인한다.

### 시나리오 F: Visual Studio 강제 종료

1. 여러 하위 작업이 포함된 대화를 실행한다.
2. 작업 관리자 등을 통해 Visual Studio를 강제 종료한다.
3. 관련 프로세스가 즉시 함께 종료되는지 확인한다.
4. 남은 프로세스가 파일 또는 외부 시스템을 계속 변경하는지 확인한다.
5. Visual Studio를 다시 시작했을 때 이전 작업이 자동 재개되는지 확인한다.

### 시나리오 G: 종료 중 오류와 지연

1. 응답이 늦거나 중단된 도구 호출을 만든다.
2. 취소 또는 Visual Studio 종료를 수행한다.
3. 종료가 무한 대기하지 않는지 확인한다.
4. 제한 시간 이후 안전한 강제 정리가 수행되는지 확인한다.
5. UI와 로그가 실제 결과를 성공, 실패, 강제 종료로 구분하는지 확인한다.

## 수집해야 할 진단 정보

원인 분석 단계에서는 각 재현마다 다음 정보를 함께 수집할 필요가 있다.

- Visual Studio 프로세스 ID
- LIG VS Sidecar 프로세스 ID
- LIG VS가 시작한 것으로 의심되는 자식 및 손자 프로세스 ID
- 대화 세션 ID와 작업 ID
- 중지, 취소, 뒤로가기, 창 닫기, Visual Studio 종료 시각
- 각 프로세스의 시작 시각과 종료 시각
- 마지막 SDK 이벤트와 마지막 도구 이벤트
- 실행 중이던 터미널 명령의 상태
- MCP 및 Hook 실행 상태
- 종료 요청 접수 여부
- 종료 완료 확인 여부
- 종료 실패 또는 제한 시간 초과 여부
- Visual Studio 재시작 후 세션 상태

로그에는 프롬프트 원문, 인증 정보, 명령 출력 전체와 같은 민감한 내용을 불필요하게 남기지 않고 수명 주기 확인에 필요한 식별자와 상태만 기록해야 한다.

## 완료 판정 기준

다음 조건을 모두 만족해야 수정 완료로 판단할 수 있다.

1. 중지 또는 취소 완료 표시 이후 해당 대화의 추가 응답과 도구 실행이 발생하지 않는다.
2. 취소된 작업이 파일이나 외부 시스템을 더 변경하지 않는다.
3. 관련 터미널 명령의 자식 및 손자 프로세스까지 남지 않는다.
4. 세션 나가기 또는 뒤로가기의 백그라운드 실행 정책이 명확하고 UI 상태와 실제 상태가 일치한다.
5. LIG VS 도구 창 닫기 후의 동작이 제품 정책과 일치한다.
6. Visual Studio 정상 종료 후 LIG VS 관련 작업과 프로세스가 남지 않는다.
7. Visual Studio 강제 종료 후에도 고아 프로세스가 남지 않는다.
8. 종료 실패나 강제 종료를 정상 완료로 잘못 표시하지 않는다.
9. Visual Studio 재시작 후 종료된 작업이 사용자 모르게 다시 진행되지 않는다.
10. 연속 취소, 반복 종료, 종료 중 재실행에서도 중복 작업이나 교착이 발생하지 않는다.
11. Visual Studio 2022 17.0과 17.12 패키지 모두 같은 시나리오를 통과한다.

## 개발팀 확인 요청

최신 배포본과 최신 저장소 코드를 기준으로 다음 사항을 별도로 확인해야 한다.

1. UI의 각 종료 동작이 실제 SDK 실행까지 취소를 전달하는가?
2. SDK 취소 완료를 확인한 뒤 UI에 완료를 표시하는가?
3. 터미널과 도구가 만든 하위 프로세스 전체가 종료되는가?
4. LIG VS 도구 창 닫기가 실제 종료인지 단순 숨김인지?
5. Visual Studio 패키지 종료 시 Sidecar의 정상 종료 완료를 기다리는가?
6. 정상 종료가 실패할 때 제한 시간과 강제 정리 경로가 있는가?
7. Visual Studio 강제 종료에서도 관련 프로세스가 남지 않도록 운영체제 수준 보호 장치가 있는가?
8. 비정상 종료 후 이전 세션을 어떻게 복구하거나 차단하는가?
9. 종료 실패와 잔존 프로세스를 진단 로그에서 확인할 수 있는가?

## 최종 요청 사항

이 문제는 UI에서 중지 또는 창 닫기가 보이는지만 확인해서는 안 된다. 실제 SDK 작업, 도구 호출, 터미널 명령, Sidecar 및 모든 관련 하위 프로세스가 함께 종료되는지를 프로세스와 부작용 기준으로 검증해야 한다.

특히 Visual Studio 정상 종료와 강제 종료를 별도 시나리오로 시험하고, 화면이 닫힌 뒤 작업이 계속되지 않는다는 것을 반드시 보장해야 한다.
