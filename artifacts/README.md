# Artifacts

Build outputs consumed by VSIX packaging live here.

- `WebApp/` is generated by `src/webview` and remains tracked for release packaging.
- `Sidecar/` and `.staging/` are generated by `src/sidecar` and are ignored.
