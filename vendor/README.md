# Vendor

Offline and machine-specific dependencies live here, outside the source tree.

- `LocalPackages/` is the tracked offline NuGet feed.
- `WebView2Runtime/` is an ignored, optional fixed WebView2 runtime.
- `installers/` contains third-party prerequisite installers used by setup scripts.
